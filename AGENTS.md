# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

The project is pinned to SDK 54 because it is the last SDK with a published Expo Go client.
SDK 55+ requires a development build. Do not bump the SDK without switching the run target first.
